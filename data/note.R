# ici, mettre des fichiers .rda permettant de tester le package. utiliser des extraits de données resourcecode.
# documenter les jeux de données exportés dans R/data.R

# Données exportées (accessibles via data())
# usethis::use_data(mon_dataset)

# Données internes (pas visibles par l'utilisateur)
# usethis::use_data(lookup_table, internal = TRUE)

# Créer le script de préparation des données
# usethis::use_data_raw("mon_dataset")